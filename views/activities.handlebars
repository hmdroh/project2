<div class="container" id="recipe-body">



<div class="row">
{{#each boo}}
    <div class="col s12 m6 l6">
      <div class="card">
        <div class="card-content">
          <div class="row">
            <div class="col l2" id="date">
              <p>{{local_date}} {{local_time}}</p>
            </div>
            <div class="col l8">
              <a href="/activity/{{group_url}}/{{id}}" id="title">{{name}}</a>
              <p id="location">{{#if addressFromVenue}}{{venue_address}}{{else}}{{group_address}}{{/if}}</p>
            </div>
            <div class="col l2">
              <a href="/api/fevent/{{id}}/{{group_url}}"><i class="material-icons">{{#if Fav}}favorite{{else}}favorite_border{{/if}}</i></a>
            </div>
          </div>
        </div>
      </div>
    </div>
{{/each}}
  </div>